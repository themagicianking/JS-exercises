// Task #1

let user = {};
user.name = "John";
user.surname = "Smith";
user.name = "Pete";
delete user.name;

// Task #2

function isEmpty(obj) {
  if (Object.keys(obj).length == 0) {
    return true;
  } else {
    console.log(Object.keys(obj))
    return false;
  };
}

// Task #3

function sumSalaries(salaries) {
  let salariesArr = Object.values(salaries)

  if (salariesArr.length == 0) {
    return 0;
  } else {
    return salariesArr.reduce((accumulator, currentValue) => accumulator + currentValue)
  }
}

// Task #4

function multiplyNumeric(obj) {
  for (let key in obj) {
    if (typeof obj[key] == "number") {
      obj[key] = obj[key] * 2
    }
  }
}